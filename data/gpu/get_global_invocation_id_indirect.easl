@{address storage-write
  group 0
  binding 1}
(var arr: [vec4f])

(defn helper []
  (let [i (.x (global-invocation-id))]
    (= (arr i) (vec4f i))))

@compute
@{workgroup-size 16}
(defn c []: ()
  (helper))
