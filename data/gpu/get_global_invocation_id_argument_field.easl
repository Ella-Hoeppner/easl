@{address storage-write
  group 0
  binding 1}
(var arr: [vec4f])

(struct ComputeInput
  @{builtin global-invocation-id}
  id: vec3u)

@compute
@{workgroup-size 16}
(defn c [in: ComputeInput]: ()
  (let [i (.x (global-invocation-id))]
    (= (arr i) (vec4f i))))
