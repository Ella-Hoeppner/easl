; Nested runtime-sized arrays can never be GPU buffer bindings — there is
; no flat host-shareable layout for them.
@storage-write
(var grid: [[f32]])

@{workgroup-size 1}
@compute
(defn run []
  (= ((grid 0u) 0u) 1.))
