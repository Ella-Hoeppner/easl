@{address private}
(var private-float: f32 0.)

(defn increment [@var @ref x: f32]
  (+= x 1.))

(defn increment-twice [@var @ref x: f32]
  (increment x)
  (increment x))

(defn f []
  (increment-twice private-float)
  (let [@var x: f32 0.]
    (increment-twice x)))
