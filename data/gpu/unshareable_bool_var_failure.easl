; Bools aren't host-shareable in WGSL, so a bool-typed global may not be
; touched by GPU code. (Declaring one is fine — it's an ordinary CPU
; value until a shader references it.)
(var flag: bool)

@{workgroup-size 1}
@compute
(defn run []
  (= flag true))
