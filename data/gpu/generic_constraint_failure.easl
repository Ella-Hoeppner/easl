(struct (TwoOf T)
  first: T
  second: T)

(defn (make-two-of T: Scalar)
      [a: T]: (TwoOf T)
      (let [two (TwoOf a a)]
        two))

(defn make-two-bools [a: bool]: (TwoOf bool)
    (make-two-of a))
