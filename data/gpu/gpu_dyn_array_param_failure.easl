; Functions reachable from GPU entry points may not take runtime-sized
; arrays as by-value arguments.
@storage
(var data: [f32])

(defn first-of [a: [f32]]: f32
  (a 0u))

@storage-write
(var out: [1: f32])

@{workgroup-size 1}
@compute
(defn run []
  (= (out 0u) (first-of data)))
