(defn f [v: vec3u]: f32
  (let [x 2u]
    (match v
      (vec3u 0u 0u 0u) 1.
      _ 0.)
    ; the below won't compile because it uses x in the pattern

    #_(match v
        (vec3u 0u 0u x) 1.
        _ 0.)))
