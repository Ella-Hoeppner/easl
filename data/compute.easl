@compute
@{workgroup-size 16}
(defn c []: ()
  (let [@var x 0.25]
    (+= x 0.5)))

@{workgroup-size 16}
(defn c2 []
  (let [@var x 0.25]
    (+= x 0.5)))

; the below fails because it's missing a workgroup size
#_@compute
(defn c3 []
  (let [@var x 0.25]
    (+= x 0.5)))

; the below fails because workgroup-size isn't allowed on any entry but compute
#_@fragment
@{workgroup-size 16}
(defn c3 []: ()
  (let [@var x 0.25]
    (+= x 0.5)))