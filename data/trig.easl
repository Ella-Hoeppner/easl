(defn f [a: f32]: ()
  (let [b (vec4f 0.)
        @var x 0.
        @var y (vec4f 0.)]
    (+= x (sin a))
    (+= x (cos a))
    (+= x (tan a))
    (+= x (atan a))
    (+= x (atan2 a a))
    (+= x (acos a))
    (+= x (asin a))
    (+= x (atan a))
    (+= x (sinh a))
    (+= x (cosh a))
    (+= x (tanh a))
    (+= x (asinh a))
    (+= x (acosh a))
    (+= x (atanh a))

    (+= y (sin b))
    (+= y (cos b))
    (+= y (tan b))
    (+= y (atan b))
    (+= y (atan2 b b))
    (+= y (acos b))
    (+= y (asin b))
    (+= y (atan b))
    (+= y (sinh b))
    (+= y (cosh b))
    (+= y (tanh b))
    (+= y (asinh b))
    (+= y (acosh b))
    (+= y (atanh b))))