(defn call-twice [g: (Fn [] f32)]: f32
  (g)
  (g))

(defn f []: f32
  (let [@var counter 0.
        offset (vec3f 1. 2. 3.)]
    (call-twice (fn []
                    (+= counter offset.z)
                    counter))))
