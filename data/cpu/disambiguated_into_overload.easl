; The built-in `into` conversions are selected by the expected result
; type: the same expression converts to different targets under
; different ascriptions.
@cpu
(defn main []
  (print ~5u: f32)
  (print ~5u: i32))
