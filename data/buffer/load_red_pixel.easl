@handle
(var tex: (Texture2D f32))

@storage-write
(var value: vec4f)

@cpu
(defn main []
  (= tex
     (load-image "red_pixel.png"))
  (dispatch-compute-shader (fn []
                               (= value
                                  (texture-load tex (vec2u 0u) 0u)))
                           (vec3u 1u))
  (print value))

